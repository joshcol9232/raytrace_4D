use nalgebra::{Vector4};

struct Ray {
    n: Vector4<f32>, // Direction of travel
    p: Point4<f32>,  // Point on the view plane
}


fn generate_viewport_rays(view_dir: Vector4<f32>, fov: f32) {
    // Find viewport distance
    let v = 
}

fn main() {
    println!("Hello, world!");
}
